---

- name: "YouTrack | Virtual host | Apache2 | Make available"
  become: yes
  become_user: root
  template:
    src: "../templates/vhosts/apache2.conf"
    dest: "/etc/apache2/sites-available/youtrack.conf"
    owner: root
    group: root
    mode: 0777

- name: "YouTrack | Virtual host | Apache2 | Make enabled"
  become: yes
  become_user: root
  template:
    src: "../templates/vhosts/apache2.conf"
    dest: "/etc/apache2/sites-enabled/youtrack.conf"
    owner: root
    group: root
    mode: 0777